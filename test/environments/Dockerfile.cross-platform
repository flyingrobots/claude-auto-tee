# Placeholder Dockerfile for CI compatibility
FROM alpine:latest

# Install bash for testing
RUN apk add --no-cache bash

# Copy source code
COPY . /app
WORKDIR /app

# Make test script executable
RUN chmod +x test/graceful-degradation-test.sh

# Default command runs our bash tests
CMD ["bash", "test/graceful-degradation-test.sh"]